{
  "name": "aicoachellavalley-org",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/prepare-feeds.mjs && astro dev",
    "build": "node scripts/prepare-feeds.mjs && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "comment": "Versions are PINNED EXACT, not caret-ranged. `^6.1.3` resolves to 6.4.8 today, which would put .org on a different Astro than .com (6.1.3) — the opposite of the one-mental-model rule this pin exists to serve. Bump deliberately, both repos together.",
  "dependencies": {
    "@astrojs/mdx": "5.0.3",
    "@astrojs/rss": "4.0.19",
    "astro": "6.1.3"
  }
}
